#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main() {
	int num;
	int test, s, b;
	int arr[504] = { 0 }; // 모든 경우의 수

	scanf("%d", &num);
	
	for (int i = 0; i < num; i++) {
		s = 0;
		b = 0;
		scanf("%d %d %d", &test, &s, &b);

	}

	return 0;
}